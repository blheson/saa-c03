
#!/usr/bin/env bash
set -e

echo "== list objects =="
# Check for bucket name
if [ -z "$1" ]; then
  echo "Usage: $0 <bucket-name>"
  exit 1
fi

BUCKET_NAME=$1

# https://docs.aws.amazon.com/cli/latest/reference/s3api/list-bucket.html#examples
aws s3api list-objects-v2 \
--bucket "$BUCKET_NAME"